# QA Report v2.0b.2 – Layout Cleanup

## Fokus
- Entfernt veraltete Roadmap-/Strategiebox aus dem Hauptbereich.
- Topbar-Fallbadge gefüllt oder defensiv ausgeblendet, wenn kein aktiver Fall existiert.
- Asset-Zugriff für Topbar/Branding auf lokale `ASSETS`-Konstante korrigiert.
- Dashboard-Zähler nutzen die lokale Fallbibliothek (`cases`) statt `window.cases`.

## Geprüft
- HTML enthält keine Roadmap-Box mehr.
- JavaScript-Syntaxcheck bestanden.
- Vollständige v2.0b.1/v1.8e-Funktionalität bleibt als Basis erhalten.
