# Installation v2.2.4m

1. Aktuelle funktionierende `index.html` sichern.
2. Neue `index.html` aus diesem Paket in den bestehenden `v3`-Ordner kopieren.
3. Vorhandene `index.html` ersetzen.
4. Simulator normal über `index.html` starten.

Nur die neue `index.html` ist für den Patch erforderlich.
