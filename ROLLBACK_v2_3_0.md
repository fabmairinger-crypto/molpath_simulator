# Rollback — v2.3.0 UI Polish

Replace `index.html` with the previous v2.2.4s `index.html`. No data migration or storage migration is required; this patch changes only presentation/runtime UI labeling.
