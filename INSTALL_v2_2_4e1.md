# Installation v2.2.4e1

1. Die aktuell verwendete `index.html` sichern.
2. Die neue `index.html` aus diesem Paket in den bestehenden v6-Ordner kopieren.
3. Die fehlerhafte v2.2.4e-`index.html` ersetzen.
4. Den Simulator weiterhin über `index.html` starten.

Nur die neue `index.html` ist für den Hotfix erforderlich.
Die übrigen Dateien dienen der Nachvollziehbarkeit.
