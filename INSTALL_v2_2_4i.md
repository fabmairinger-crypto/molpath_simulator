# Installation v2.2.4i

1. Back up the current working `index.html`.
2. Copy the new `index.html` into the existing v6 folder.
3. Replace the previous `index.html`.
4. Start the simulator as before by opening `index.html`.

Only the new `index.html` is required. The remaining files provide traceability.
